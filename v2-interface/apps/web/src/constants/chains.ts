import { BackendApi } from '@universe/api'
import { UniverseChainId } from 'uniswap/src/features/chains/types'

export const DEFAULT_CHAIN_ID = UniverseChainId.Kasane
export const DEFAULT_GQL_CHAIN = BackendApi.Chain.MonadTestnet
